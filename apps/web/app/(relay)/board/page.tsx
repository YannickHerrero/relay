export default function BoardPage() {
  return (
    <section className="relay-empty-page">
      <p className="kicker">Workboard</p>
      <h1>Your delivery workflow starts here.</h1>
      <p>Register a repository, then turn a vague request into an approved implementation.</p>
    </section>
  );
}
